# URL de l'API backend (doit se terminer par /api)
VITE_API_URL=http://localhost:3001/api

# Checklist locale :
# 1. MySQL Laragon + base unipath
# 2. unipath-api/.env configuré (DATABASE_URL, JWT_SECRET)
# 3. npm run dev dans unipath-api puis unipath-front
